import 'package:flutter/material.dart';

class DemoProjectsPage extends StatelessWidget {
  const DemoProjectsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigoAccent,
    );
  }
}
